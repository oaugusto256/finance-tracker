<%= form_with(model: user_stock, local: true) do |form| %>
  <% if user_stock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_stock.errors.count, "error") %> prohibited this user_stock from being saved:</h2>

      <ul>
      <% user_stock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :user_stock_user_id %>
  </div>

  <div class="field">
    <%= form.label :stock_id %>
    <%= form.number_field :stock_id, id: :user_stock_stock_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
